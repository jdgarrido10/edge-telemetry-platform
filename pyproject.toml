[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [
    ".venv",
    "grafana_data",
    "influx_data",
    "mosquitto_data",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
ignore = ["E501"]